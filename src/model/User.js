class User {
    constructor(credentials, person, userId) {
        this.credentials = credentials // Credentials
        this.person = person // Person
        this.userId = userId // String
    }
}

export { User }
